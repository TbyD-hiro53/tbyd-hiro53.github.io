# h!ro53 — 公開形式・保存方針

更新日：2026-09-06
承認：作者Ryotaが、単一HTMLの義務を廃止し、作品に応じた形式選択・一式保存へ移行する提案と公開案内の改訂を承認。
対象：ブラウザで公開するビジュアル作品の実装・保存・引き継ぎ。

## 適用範囲

本裁定は、従来の「全作品を単一HTMLに格納」「依存物をすべてHTMLへ埋め込む」「公開版の外部scriptは0」という一律の条件を置き換える。
ここで許可する外部scriptは、HTMLとは別ファイルとして同じ公開サイトに同梱するスクリプトを含む。
軽量作品では単一HTMLを優先し、精細な3Dや多量の画像・音声を用いる作品では分割方式を選べる。既存の承認済み作品を一律に変換しない。

Three.js r128固定、作品スクリプトのIIFE、ES modules・importmapの禁止、既存禁止API、個別作品の造形・運動・色・閲覧要件は、本裁定では変更しない。
世界設定・小説本文・永久非開示の裁定にも影響しない。

## 公開と保存

- 描画に必要なライブラリ・画像・立体データ等は原則として自分の公開サイトに同梱する。分割方式の許可は、第三者CDNや外部サービスへの依存追加を一括承認するものではない。
- 既存のアクセス計数画像1件は従来の例外として扱う。「外部リクエストを一切行わない」とは表示しない。
- 分割作品も、再現に必要なファイル・構成一覧・起動方法を一つの保存ZIPへまとめる。複数作品で共有する依存物も、当該作品の保存一式に含める。
- ファイル欠落と参照先を検査し、保存ZIPを別の場所に展開して動作確認する。未実施の確認を完了と報告しない。
- ZIP保存と、HTMLを直接開くオフライン再生を区別する。ローカルサーバー等が必要なら起動方法に記載する。オフライン鑑賞は作品ごとの要件として検証する。
- iPhone／Safariでの閲覧を基準とし、初期読み込み・操作・描画負荷を確認する。画質の向上と配信容量の増加は別に評価する。
- 公開URLのファイル名とフラット配置は従来どおり。分割素材には作品識別と版識別を付け、同時使用される別版の素材を上書きしない。
- 納品名は版ごとに新規とし、承認済み旧版を保持する。今回の文言・方針更新は、既存の全作品の再梱包・再検証を完了したという意味ではない。

## 並行作業

最新の正本とコミットを取得し、独立した作業コピーで対象箇所を更新する。
公開直前に最新状態との差分を確認し、他セッションの更新を保持して統合する。古いindex.htmlやCANON_TEXT.mdを丸ごと書き戻さない。
通常のfast-forward更新を使い、並行更新があれば取得・統合をやり直す。履歴の強制上書きを行わない。

## 今回の反映

- index.htmlのViewing日英と、検索・SNS共有用の説明3か所を改訂する。
- 公開文言の正本はCANON_TEXT.mdに保存し、build_frag.py --viewing-onlyで反映する。
- Binary Dusk Web v3の分割方式は本方針で許容される。既存の作品固有の公開例外記録は制作履歴として保持する。
- GitHub内の本記録をGPT／Claudeの引き継ぎに用いる。ChatGPTのプロジェクト設定欄、添付済みの旧ZIP、Claude側の設定が自動更新されたことを意味しない。

