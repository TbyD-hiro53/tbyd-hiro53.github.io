# CHROME LITURGY / 第一次改修版

仮題。Cyberwafer v25のT、四つの文字環、固定円盤1枚と循環円盤6枚、基本配色・配置・運動を継承する別作品の改修プレビュー。世界設定・用途・物質の正式名称は追加していない。

独立プレビュー: https://tbyd-hiro53.github.io/chrome-liturgy-r1-preview.html

旧版 `chrome-liturgy.html` とその素材を保持。作品リストで2番目にある既存作品の改修候補であり、別の3番目の作品として重複登録しない。原典 `cyberwafer.html` は無変更。

## 同梱物

- `scene/chrome-liturgy-r1.blend`: 編集可能な高品質原本。必要な画像はパック済み。
- `tools/`: 原典復号、輪郭整理、Blender生成・更新、変換、描画、検証・梱包のスクリプト。
- `reference/`: 原典v25 HTML、復号形状、元文字画像、整理した輪郭、来歴。
- `repository/`: 公開用Web一式、比較ページ、共有ライブラリ。
- `renders/`: 前後比較、全景、近接、消灯、ニュートラル・斜光、Web、縦画面と実測JSON。
- `docs/`: 内部の材質規則、Cycles/Webの差、変更記録、検証結果と未決事項。

## 鑑賞

HTTPSプレビューは外出先のiPhoneから開ける。作者のMac稼働や同一Wi-Fiを必要としない。ZIP保存と、HTMLファイルを直接開く再生は別であり、WebGLの分割素材にはHTTP配信が必要。

ドラッグ・矢印キーで回転、ピンチ・ホイール・±で拡大縮小。タップで0.78秒基準の拍動。停止／再開、自動回転、画質、写真、全画面に対応。写真から立体へ戻っても視点を保持する。

## 再制作

Blender 5.2.1 LTSとPython用 `tools/requirements.txt` が制作環境。一般Pythonで `extract.py` → `contours.py` → `t_profile.py`、Blenderで `renew.py` → `tune.py` → `finalise.py` → `remesh.py` → `apply_t.py` → `freeze.py` → `export.py` → `render_suite.py` の順に実行する。`scene/baseline-v1.blend` も生成入力として同梱した。Webの生成は `web.py`。最終候補の `.blend` を開けば再生成せず編集・再レンダリングできる。画像やキャッシュの外部ダウンロードに依存する材質はない。

再生成は制作者向けの説明であり、作者が鑑賞のためコードを編集する必要はない。実測環境・未検証の範囲は `docs/03_VERIFICATION.md` を参照。

基準コミット: `138307575b0a50e2be011343e235338b7a6ecc35`。新規ファイルには `chrome-liturgy-r1` 識別を使用し、旧版を上書きしない。

© 2026 h!ro53 / deus ex machina。作品の権利は著作者に帰属。Three.jsとFXAA配布物のライセンスを同梱する。


# 第一次改修・内部制作仕様

基準: 138307575b0a50e2be011343e235338b7a6ecc35。2026-09-11に独立cloneで最新mainと一致を確認。仮題 CHROME LITURGY。世界設定・用途・物質名を確定する文書ではない。

指定三文書、再出発制作指示、3D技術規約と禁止API台帳、最新TECHNICAL_POLICY、v1制作READMEを読了。単一HTML義務は現行方針で置換。r128/IIFE/同一サイト依存物/禁止APIは維持。

## 構図と空間

主役のシルエットと原典配置を固定し、連続する低い反射面、立ち上がる内殻、開口越しの遠い第二・第三面を360度に構築する。建物用途や管理主体を説明しない。寸法は原典の相対座標であり実在単位を確定しない。照明は面の縁と大きい曲面から返し、黒い無背景へ逃げない。全周カメラは殻の内側に制限する。

## 光学的規則

| 部位 | 表層 | 内部 | 端面 | 光の入り方・返り方 |
|---|---|---|---|---|
| T | 厚みの小さい赤〜ピンクの膜。低周波の膜厚差とごく細い方向性構造 | 不透明な緻密な金属。非発光 | 研磨面。正面から角まで連続した反射、角の局所半径は原典形状から保持 | 正面は赤を残し斜視に冷たい薄膜反射が少量混じる。全体透過はさせない |
| 文字 | 密度のある青緑／白の前面、低い発光 | 暗い半金属の側面を持つ厚み | 前面直下に細い導光層。輪郭の穴と狭隙を残す | 前面、研磨縁、側面で反射幅を分ける。消灯でも実体が読める |
| 円盤 | 微細な円周研磨。上面は狭い反射、裏面は少し広い反射 | クローム系不透明材 | 元の厚み0.07内で丸い縁と小さな層境界 | 輪郭の明線と面の陰影を分離。大きな傷は加えない |
| 空間 | 滑らかだが鏡ではない暗い表面。粗さは主役より高い | 厚みのある連続曲面 | 大きい開口では面が奥へ回り込む | 床は視線角と距離で反射幅を変え、遠い面の明度・細部は抑える |

## 比較と未決

v1と同じ視点・1.2秒を基準比較とする。空間と照明は変更するので材質単独比較とは呼ばない。ニュートラル・斜光・消灯確認を別に記録する。Cycles/Webは同じカメラ位置・画角・運動位相を指定する。

未決: 題名と作者による採否。iPhone/Safari実機検証は使用環境が得られない限り未実施とする。Web近似の差、実測性能、ZIP展開検査は別紙で記録する。

## 原典UVの補正

v1の画像上下余白除去は、字形を環の全高へ引き伸ばしていた。今回の「比率・配置を保持」の指示に合わせ、輪郭計算時だけ画像をクロップし、3D化では元の512px画像のV座標へ戻す。元のアルファコア、字幅、方位、環中心・半径・速度を維持し、字面高を原典へ戻す。前後比較で文字が細く見えるのは意図した比率補正であり、欠落ではない。


# CyclesからWebへの移植と差

作品原本はBlender 5.2.1 LTSのCyclesシーン。Webは同一評価メッシュをThree.js r128で表示する。写真と立体は別の描画結果であり、切り替えを隠さない。

| 材質 | Cyclesの要素 | Webの再現・近似 | 残る差 |
|---|---|---|---|
| T | 不透明金属、微細方向性、粒領域ごとの異方性方向、225–255 nmの薄膜、コート | 同じ正面・縁形状。局所座標に固定した微細色変動、領域パターンと視線角に応じた3波長の膜反射近似。環境PMREMと動的局所反射 | 完全なスペクトル薄膜や異方性BRDFではない。金属粒の反射強度と色相、面光源の反射形状に差がある。模様を単一写真に固定していない |
| 文字 | 元字形の閉じた立体。緻密な前面、暗い側面、0.005の細い導光層、0.0012の面取り | 同じ面取り済み形状と材質区分。前面と側面の反射幅・色・発光を分離 | 導光層から他の面へ入る間接光はCyclesの方が正確。Webでは局所反射に文字色が入るが、多段の光輸送を再現しない |
| 円盤 | 厚み0.07内の上下面、丸い縁、層境界。微細な円周研磨 | 同じ断面形状、表裏と端面の別材質。微細な円周模様には画素幅による減衰。T・文字の動的反射を加算 | 動的反射は中心プローブで近似するため、位置ごとの視差・粗さに応じた反射の厳密な広がりは異なる |
| 空間 | 360度の連続面と内殻、開口の奥に中層・遠層。間接光と粗い床反射 | 同一メッシュを全周表示。Cycles環境光をPMREM化。床は実際のカメラに追従する平面反射、視線角と距離で反射量・幅を変える | Webの床は一段の反射で、多重反射や全ての間接影は再現しない。遠景の明度勾配にも差が残る |

## 照明と負荷

面光源がパノラマカメラへ直接写らないことを補うため、環境光の出力時だけ、面光源と同じ位置・寸法・放射強度の円形代理面を使用する。公開モデルに撮影パネルを追加したという意味ではない。動的反射はT用と円盤用の128角キューブを交互に更新する。

標準: モバイル相当30 fps上限、DPR最大1.2、床反射約10 Hz、局所反射は交互に約0.55秒間隔。高精細: DPR最大1.5、床反射約33 Hz、局所反射は交互に約0.18秒間隔。環境光のPMREMは初回だけ生成し、毎フレームの再生成を避ける。CPU/GPUの速度は端末による。実測は検証文書に分ける。

原典・旧版からの変更: HemisphereLightを除去。モバイルのantialias:falseを維持し、同梱r128のFXAAで画素の階段を抑える。Bloom、フォグ、被写界深度は使用していない。材質と輪郭の問題をこれらの効果で隠さない。

カメラの初期フィットはheader/title/controlsの実際の矩形から計算する。画質切替では現在のズーム比を保ち、正面へボタンだけが初期フィットを明示的に復元する。横持ちには専用のUI余白を設ける。全画面APIが使えない場合はUIを減らした表示と復帰ボタンを用意する。

Liquid Glassの色、境界、内側ハイライト、22px blur/saturate/brightnessは現行The Confluence v5の実ファイルと照合した。同じトークンを使用する。


# 第一次改修版の検証 / 2026-09-11

今回実行した検査を記す。旧READMEの記載を今回の実施結果として転記していない。基準mainは `138307575b0a50e2be011343e235338b7a6ecc35`。原典、再出発パッケージの00/02/03、現行TECHNICAL_POLICY、3D規約・禁止API台帳、旧版の制作記録を照合した。

## 環境と測定の限界

Blender 5.2.1 LTS / macOS / Apple M2 Max。最終CyclesはCPU 8スレッド、112サンプル、適応閾値0.016。ブラウザはCodex IABのChromium、ANGLE Metal / Apple M2 Max。390×844と844×390はCSS viewportを実際に変更して撮影した。DPRは1であり、iPhone実機のDPR・Safari・タッチ感度の再現とは称さない。Blender描画とブラウザ測定は同じMac上で一部並行した。

| 最終表示 | フレーム間隔 中央 / P95 | JS描画投入時間 中央 / P95 | JS heap |
|---|---|---|---|
| 縦390×844 標準 | 33.2 / 34.3 ms | 0.30 / 0.70 ms | 96.5 MB |
| 縦390×844 高精細、停止 | 33.2 / 34.3 ms | 0.40 / 0.90 ms | 93.7 MB |
| 比較1000×1200、停止 | 8.3 / 9.6 ms | 0.10 / 0.30 ms | 93.8 MB |
| 横844×390 高精細、停止 | 8.3 / 9.8 ms | 0.20 / 0.70 ms | 134.5 MB |

直近300フレームの値。停止中も反射を含む描画は続く。JS投入時間はGPU描画時間ではない。標準縦表示は約30fps、比較画面は約120fpsの更新。DPR1のこの環境では画質切替による画素数差は生じず、反射更新頻度が主な差になる。WebGL幾何13、テクスチャ6、メイン・影・FXAA合計74 draw calls / 324,442 triangles。オフスクリーン反射の回数は別途JSONに記録。GPUメモリの実測値は取得していない。ヒープの増減だけでリークがないとは断定しない。

## 原典・形状・運動

`verification.json`: 円盤14,414条件の原典照合、最大誤差2.23e-16未満。拍動10,000条件一致。0.78秒、2.4秒、2.38秒、14.4秒と四環速度[-0.42,0.60,-0.50,0.36]を原典ソースと照合。文字列と元PNGを保持。14.4秒は円盤のみの循環。

`native-verification.json`: 15フレーム、最大移動誤差3.82e-7未満。循環の境界で完全に重なる円盤を表示しない処理も確認。T浮遊8秒と各文字速度は独立。

`geometry-check.json`: T、円盤原型、四文字環の非多様体エッジ0。Tの平面法線の最大誤差1.20e-7未満。Tの原典断面寸法を保持し、角のRと接続を変更。完全な旧頂点一致ではない。ニュートラルと斜光で内角のハイライトを目視した。

評価後のWeb形状データは4,351,440 bytes / 132,908 triangles。旧版は19,740,788 bytes / 536,911 triangles。四文字環のWeb頂点は409,670から71,529へ整理。ポリゴン削減自体を品質の達成条件にはしていない。円盤の厚み0.07、半径と薄い輪郭を保持。文字側面には元画像の角張りに由来する細かな面の変化が一部残る。

カメラは全周、距離7–38、角度下限をその地点の立ち上がる床に合わせる。`verification.json` の63,360条件で内殻床との余裕最小0.64982。`geometry-check.json`内のcamera欄は平床に対する参考値であり、曲面の全周検査は前者が正式結果。

## ブラウザ操作

- 通常ロード成功、最終通常画面のコンソールエラー0。故意の素材欠落テストでは期待した1エラーを記録し、写真と再読込リンクを表示。
- 停止で位相が止まり、再開で進む。ドラッグ・矢印による回転、±によるズーム、正面復帰を確認。タップで最大1.03626の拍動を観測。
- 画質切替でカメラ・距離・停止位相を保持。写真を1800×2160でデコードし、立体へ復帰。自動回転も保持。
- 全画面要求が成功扱いでも実表示が変わらないIABの挙動を確認。1.2秒後の実状態検査により代替表示へ移行し、復帰ボタンで戻る最終版を確認。ネイティブ全画面そのものの成功はこの環境では未確認。
- 縦390×844のタイトルY66/H34、操作部Y762/H44。横844×390のタイトルY8/H23、操作部Y320/H44。表示領域内に収まり、通常画面で主役との重なりを画像で確認。
- 同一視点・位相、側面、背面、反対側を撮影。環境形状が背面にも続く。静止・回転で目立つ点滅、初期のコンパイル失敗は見られなかった。境界重複のちらつきは円盤の表示処理で抑制。
- 床と局所反射の更新カウンタ増加を確認。粗い反射と局所プローブは近似であり、急な回転や移動では追従の遅れ・位置視差が残り得る。Cyclesとの差は比較画像で公開する。

物理的ピンチ・ホイール、iPhone/Safari実機、低性能端末、長時間の発熱・GPUメモリ、実際のWebGL context lossからの復旧は未検証。ポインタ・ホイール処理の数式検査を別JSONに記録しても、実機操作の代わりとは扱わない。

## 最終画像と梱包

全10ショットを同一の最終 `.blend` SHA-256から描画。全景1800×2160は240.02秒、広角1440×900は71.05秒、T近接114.88秒、デノイズなし108.50秒、文字98.50秒、円盤87.73秒。プロセス最大RSSは約8.36 GB（累積最高値であり、各画像固有のメモリ量ではない）。全実測とカメラは `renders/render-measurements.json`。

デノイズ有無を並べて確認。縁は残り、細かな粒状ノイズは消えるが、極細の粒感も一部平滑化される。撮影画像には生成AIによる上描き、後付けの質感加工をしていない。比較シートは縮小と配置のみ。

ZIPには現原本と生成入力の旧原本、元素材、全スクリプト、Webと共有Three.js、比較画像、実測JSONを含む。`manifest-sha256.json`で全同梱ファイルを照合できる。別ディレクトリ展開・HTTP起動の実検査は `renders/archive-verification.json` に記録する。作者は `OPEN-ME.html` からHTTPS版と同梱比較画像を開ける。制作者がローカル配信する場合はPythonで `tools/serve.py` を実行する。

公開は新規 `chrome-liturgy-r1-*` ファイルのみの独立プレビュー。旧版・原典・作品一覧の2番目という既存配置を維持。正式採用と題名は未決。仮題を維持し、将来の題名案として「CHROME LITURGY」「SILENT STRATA」「ROSE / CHROME」を残す。これらは設定や物質名を定義しない。

配布前に画像のファイルパス等のメタデータを除去し、デコード後の画素ハッシュが不変であることを確認。Blenderは形状・材質・運動を変更せず、パック画像と出力のパスを相対化した可搬コピーをZIPへ入れた。描画時原本と配布原本のハッシュ対応は `portable-blend-verification.json`。


# 変更記録 / R1

基準はGitHub main `138307575b0a50e2be011343e235338b7a6ecc35`。独立コピーで制作。公開版のv1と原典、作品一覧、他作品は変更しない。新規ファイルだけを追加するレビュー用公開。

1. 暗い壁・床の撮影室を、全周の連続面・立ち上がる内殻・中層と遠層・開口へ置換。開口と反射面はカメラの裏にも存在する。人物・配管・手すり・施設設定は加えていない。
2. Tの広い面の法線を分離し、重複面取りを除去。近接検査で残った内角の折れを解消するため、原典の断面寸法を抽出して縁を再構成。幅4.2、高さ3.5–8.27、厚み1.05、軸部半幅約0.709976を保持。深さ方向の縁R0.028、外角R0.035、内角R0.045を起点に、12分割の曲面と解析的法線を使用。外形境界の最大距離約0.0232は主に角Rの改修による。完全な頂点一致とは称さない。
3. Tの赤〜ピンクを、微細な方向性を持つ金属、粒領域ごとの弱い反射方向差、225–255nmの薄膜へ再設計。全体をガラスや発光体にしていない。強すぎた木目状の色変動とモザイク状の反射は不採用。
4. 文字は元のアルファコアから輪郭と穴を抽出し、閉じた前後面・側面・導光層へ再構成。曲面の細分境界を共有して微小な隙間を防ぐ。側面法線は曲線に沿わせ、急な角は保持。輪郭R0.0012。元の文字数・穴・角度配置を保つ。
5. 原典UVを再照合し、v1が上下余白除去時に拡大していた字面高を補正。輪郭処理用のクロップ座標を元画像512pxのV座標へ戻す。比較画像で字面が低く見えるのは原典比率への復帰。
6. 円盤は元の半径・厚み0.07を保つ回転断面として再構成。上面・裏面・丸い縁・小さな層境界・円周研磨を分ける。構成枚数と循環式を保持。
7. Cyclesの面光源・空間内の返り光を再設計。ニュートラル、斜光、文字消灯の診断画像を追加。Bloom、フォグ、被写界深度なし。
8. Webへ同じ評価メッシュと材質区分を出力。視線角で変化する薄膜近似と限定した金属粒表現、更新する局所反射、床の平面反射を実装。床の遠い境界では平面反射を滑らかに減衰させる。差は `02_CYCLES_WEB.md` に明記。
9. HemisphereLightを削除。r128/IIFE/同一サイト依存を維持。FXAAを同梱。画質切替の視点保持、実UI寸法からの初期フィット、横持ちUI、全画面の代替表示を追加。
10. カメラを引いて低い角度へ回した際、立ち上がる床へ入らないよう、その位置の曲面高に合わせて下限角を制御。63,360条件で原典座標約0.65以上の余裕を確認。

全体の14.4秒ループは作っていない。14.4秒は円盤循環であり、T浮遊8秒と文字環の各速度は別。ネイティブの1–865フレームを全体シームレスループと表示しない。

未決は題名と作者の採否。iPhone/Safari実機、物理的なマルチタッチの操作感、実機の発熱・GPUメモリは未検証。ブラウザのエミュレーション測定と区別する。
