# CHROME LITURGY（仮題）

Cyberwafer v25 の構造・基本色・運動を継承する、独立したBlender作品。作品一覧では原典の直後、2番目に配置する。

## 鑑賞

`repository/chrome-liturgy.html` をローカルHTTPサーバー経由で開く。ドラッグ／矢印キーで回転、ピンチ／ホイール／±で拡大、タップで拍動。停止・自動回転・高精細表示・写真・全画面・保存に対応する。

編集原本は `scene/chrome-liturgy-master.blend`。Blender 5.2.1 LTSで制作。画像はパック済み。原本は60 fps、1–865フレーム。円盤の14.4秒周期を収録しているが、文字環とTの周期は別なので、865フレームから1へ戻すと作品全体は連続しない。ブラウザ版は連続時間で再生する。

## 継承と制作

- Tと円盤はv25の埋め込み形状を直接復号。Tに0.012、円盤に0.003の微小エッジ処理。
- 文字はv25の発光のにじみを除いたアルファコアから構築した0.038厚の立体。文字列・配置・半径を保持し、画像専用の上下余白を除去して、元のリングの高さの中で字形が読めるようにした。
- Tのピンク〜赤、文字の青緑と白、クローム円盤という基本色を維持。
- TはPVD金属を想定した薄膜・コート・微細な表面。円盤は円周研磨、微細法線と粗さの変動を追加。
- 黒い研磨石の床、暗い壁、複数の大きな面光源を使用。照明板は金属に反射し、カメラには映らない。
- 形状座標のスケールは原典と同じ。実物としての絶対寸法・世界設定の新規裁定は行っていない。

T浮遊は振幅0.18／8秒。文字環は −0.42、+0.60、−0.50、+0.36 rad/s。円盤スロットは2.4秒、移動区間2.38秒、循環14.4秒。タップ拍動は0.78秒の二峰性波形と減衰を原典から継承。タップはブラウザの入力操作なので、Blenderの通常タイムラインにはアイドル動作を記録している。

周期の開始時に動く円盤が固定円盤と完全に一致する瞬間は、重なった面だけを描かず、黒い反射やちらつきを防ぐ。軌道とスケールの数式は変更していない。

## ブラウザ表示の範囲

Three.js r128、IIFE、同一サイトの分割ファイルで構成。Blenderで評価した形状、Cyclesでベイクした粗さ・法線、CyclesのHDR環境照明を使用する。金属の環境反射は部位別のHDRプローブを用いる。床の柔らかな拡散光はCyclesでベイクし、反射は平面反射をぼかして近似する。ブラウザでは周回文字とTの相互反射を完全には再現しない。WebGLのリアルタイム表示とCyclesのパストレーシング写真は同一ではない。薄膜干渉と異方性の厳密な描画はBlender原本側に残る。

原典のY2K切り替えは原典に残す。本作は一つの材質表現として独立。新しい世界設定・小説断章は追加していない。

## 再制作

`tools/extract.py` → `tools/glyphs.py` → Blenderで `tools/build.py` → `tools/refine.py` → `tools/finish.py` の順。Python側にNumPyとPillowが必要。Metal GPUを最初に試したところ、他作業とメモリが重なり不足したため、最終写真とベイクはCPU／8スレッド前後で制作。シーン全体の設定や他作品の処理は変更していない。

ブラウザ素材は `tools/export.py`、`tools/bake.py`、`tools/bake_t_probe.py`、`tools/bake_floor.py` で書き出す。作品リストへの差分は `tools/stage.py`。元データは `reference/cyberwafer-v25.html` と来歴JSONを参照。

## 確認

`renders/verification.json`：円盤運動14,414点と拍動10,000点を元v25と数値比較、形状の値・インデックス・ファイル長・Three.js版とシェーダー挿入位置を確認。

`renders/native-verification.json`：Blender原本の15フレームを数式と比較。Cycles画像を実際に描画し、照明板の映り込み、文字の面、床の粗さを修正。ブラウザで表示・停止再開・ドラッグ・写真切替・画質変更・保存リンクを確認し、390×844の縦長表示も確認。iPhone/Safari実機での操作確認は未実施。

© 2026 h!ro53 / deus ex machina。作品の権利は著作者に帰属。Three.jsはMIT License。
